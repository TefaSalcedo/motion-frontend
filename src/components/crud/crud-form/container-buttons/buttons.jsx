import React from 'react';

const Buttons = () => {

    return (
        <div>
            <button>cancelar</button>
            <button>crear</button>
        </div>
   
    );
}

export default Buttons;
